import { CustomHttpResponse } from './custom-http-response';

describe('CustomHttpResponse', () => {
  it('should create an instance', () => {
    expect(new CustomHttpResponse()).toBeTruthy();
  });
});
